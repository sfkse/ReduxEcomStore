import React from 'react'
import loading from '../assets/images/loading.gif'

const Loading = () => {
    return (
        <>
            <img src={loading} alt="Loading" className="shop__loading" />
        </>
    )
}

export default Loading
